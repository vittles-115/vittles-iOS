# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Vittles' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
	#pod 'Firebase'
	pod 'Firebase/Core'
	pod 'Firebase/Database'
	pod 'Firebase/Auth'
	pod 'Firebase/Storage'

    pod 'Pages'
	pod 'AlamofireImage', '~> 3.0'
    pod 'Kingfisher', '~> 3.0'
    pod 'Cosmos', '~> 7.0'
    pod 'PromiseKit', '~> 4.0'

  # Pods for Vittles

  target 'VittlesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VittlesUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
